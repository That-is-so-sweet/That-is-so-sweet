# UX 回饋改進計畫（2026-08）

使用者測試後給的完整體驗回饋，原始 9 項 + 後續會議補充 3 項，共 12 項。因為橫跨多個獨立流程與桌面/行動兩套 UI 樹，拆成 4 個子專案（A/B/C/D）分別執行。這份文件是追蹤與記憶延續用的：每完成一個子專案，回來把對應項目勾掉，並在「執行紀錄」補上實際改動內容。

> 背景补充：桌面版 `src/components/EventView.tsx` 直接 import 並重用行動版的 `VoteTab`／`HeatmapTab`／`HostTab`／`CommentBoard`，不是完全獨立的兩套實作（跟 `ShareModal`/`Toast` 各自獨立的情況不同）。所以子專案 B、C 的改動大部分只需要改一次，桌面版會自動套用；只有外層排版（Tab 列、外框）需要分開調整。子專案 D（建立活動流程）桌面與行動版是分開的元件，需要各改一次。

---

## 原始需求清單（9 項）

勾選狀態＝實際改動是否已完成（對應到下面子專案的狀態）。

- [x] **1. 時間/時段選取模式簡化**：建立活動時，切換「直選日期」與「需選時段」目前用 Tab 元件；改成在下方提供「新增時段」按鈕，點選後自動帶入時段設定，介面更乾淨。→ 歸在**子專案 D（已完成）**
- [ ] **2. 新增候選時段操作優化**：同一天要新增多個時段（如 10 點、11 點）目前得把已選的 10 點改成 11 點再加一次，操作不直覺，也會讓人擔心原本的時段消失。建議把「已新增時段」清單跟「新增候選時段」合併，畫面直接列出當天已新增的時段，旁邊用 + 號或輸入框新增。→ 歸在**子專案 D**（調查發現現有程式碼看起來已經是合併呈現、不會覆蓋既有時段，可能是先前 commit 已處理；待使用者實測確認後再決定是否需要微調）
- [ ] **3. 降低單一頁面資訊密度**：體驗示範中單一頁面塞了投票、熱點圖、留言板等太多功能，讓人眼花撩亂。不一定要拆頁，但要透過元件尺寸、版面排版、顏色降低壓迫感；移除不必要的（類似 AI 自動生成的）指引小字；Demo 展示挑重點畫面呼應四大核心特點（跨平台免登入、智慧橋時間、資訊同步、自動收尾）即可，不用整個 RWD 流程都展示。→ 歸在**子專案 C**
- [ ] **4. 留言板提示訊息**：留言板無須登入，使用者可能亂打導致大家收到無意義的信件。應在留言處加提示：「在這邊留言會被寄信喔」。→ 歸在**子專案 C**
- [ ] **5. 留言板位置調整**：留言板直接呈現在熱力圖下方即可。→ 歸在**子專案 C**
- [ ] **6. 熱點圖與無登入識別邏輯**：免登入無法自動識別使用者，導致熱點圖頁面一直顯示「您尚未勾選時間」。建議採用 When2meet 式的「暱稱 + Email」識別：勾選時間前強制先填寫，勾選完直接進入熱點圖頁面；要修改再點按鈕跳回勾選頁。藉此取消 vote/heatmap 兩個 Tab 的切換設計。→ 歸在**子專案 B**
- [ ] **7. 主辦人定案按鈕整合**：主辦人定案按鈕直接做在熱點圖旁邊（看著熱點圖結果直接定案），不需要獨立 Tab。→ 歸在**子專案 B**
- [x] **8. 精確地點與地圖超連結**：建立活動時支援輸入精確地點，並偵測 Google Maps 連結；使用者貼上地圖連結時，自動把文字轉成該地點名稱並帶 Hyperlink，避免直接呈現冗長連結。→ 歸在**子專案 D（已完成）**
- [x] **9. 視覺與警示顏色衝突優化**：主色調橘色跟未選時段提示/危險操作等警示色（紅色）太相近，警示意義會被蓋掉、失去效果。建議微調顏色數值，或定義輔色（輻射色）處理行事曆選取等畫面，不要全部用主色。→ 歸在**子專案 A（已完成）**

以下 3 項來自後續會議紀錄的補充回饋：

- [ ] **10. 主辦人修改活動資訊功能**：與會者提出疑問——建立活動後若活動資訊有誤，主辦人要去哪裡修改？團隊確認目前完全沒有提供修改管道。需要讓主辦人有權限修改已建立的活動資訊。→ 歸在**子專案 B**
- [ ] **11. 「批次快速勾選」的 onboarding 與引導說明**：有成員提到畫面收合時，「批次快速勾選」這個標籤一開始會讓人困惑（不確定系統要怎麼幫忙勾選），雖然點開後其實好理解。不一定要在 MVP 階段做，但後續可以加上 onboarding／輔助說明引導使用者理解這項功能。→ 歸在**子專案 C**
- [ ] **12. 熱點圖頁面的視覺重心矛盾**：與會者指出，使用者尚未勾選時間時，頁面標題是「熱點圖」，但畫面最顯眼的區塊卻是一直催促「勾選時間」的按鈕，視覺重心跟頁面主題不一致。跟第 6 項（暱稱+Email 識別、取消 Tab 切換）邏輯相關，但這裡是額外被點出的視覺層面問題。→ 歸在**子專案 B**

---

## 子專案總覽

| 子專案 | 對應項目 | 狀態 | 依賴 |
|---|---|---|---|
| A. 色彩微調 | #9 | ✅ 已完成 | 無 |
| B. 活動頁面互動重構 | #6、#7、#10、#12 | 🟡 設計已完成，待寫實作計畫 | 無 |
| C. 版面密度與留言板調整 | #3、#4、#5、#11 | ⬜ 未開始 | 依賴 B 先落地 |
| D. 建立活動流程優化 | #1、#2、#8 | ✅ 已完成（#2 待使用者手動確認） | 無，可獨立插隊 |

---

## 子專案 A — 色彩微調 ✅ 已完成

**對應項目**：#9

**涉及檔案**：
- `src/design-system/tokens/colors.css`
- `src/mobile/MonthCalendar.tsx`、`src/mobile/VoteTab.tsx`、`src/mobile/HeatmapTab.tsx`（週末/假日標色）
- `src/mobile/HostTab.tsx`、`src/mobile/VoteTab.tsx`（警示卡片邊框的硬寫 rgba）

**處理內容 / 執行紀錄**：
- [x] `--color-hot` 從 `#C23B1A` 調成 `#D6303C`（明確偏紅，跟品牌橘拉開），`-dark` → `#AC2530`，`-subtle` → `#FCE9EA`
- [x] `--color-error` 從 `#E8361A` 調成 `#B91C2C`（比 hot 更深，代表更嚴重的錯誤/危險狀態），`-subtle` → `#FBE7E9`
- [x] 新增 `--color-weekend`（週末/假日日期裝飾色，跟警示色脫鉤）與 `--color-weekend-subtle`
- [x] `MonthCalendar.tsx`／`VoteTab.tsx`／`HeatmapTab.tsx` 裡 8 處週末/假日標色從 `var(--color-hot)` 改成 `var(--color-weekend)`；真正的警示用途（表單必填提示、AlertTriangle 圖示、「尚未選擇時段」角標、投票已截止狀態）維持用 `--color-hot`
- [x] `HostTab.tsx`／`VoteTab.tsx` 裡硬寫的 `rgba(194,67,26,…)` 邊框同步更新成新 hot 色值對應的 rgb
- [x] 使用者後續手動把 `--color-primary` 改成 `#e06828`、`--color-secondary` 改成 `#ffd900`，因此重新計算了連動色階：
  - `--color-primary-dark` → `#C2551A`、`-light` → `#F08950`、`-subtle` → `#FDF1E9`
  - `--color-secondary-dark` → `#D3B400`、`-light` → `#FFE454`、`-subtle` → `#FFFEEA`
  - `--color-weekend` 色相回調到 `#A1493B`（跟新主色 H≈21° 拉開距離，落在主色與警示紅中間）；`--color-weekend-subtle` → `#F5E1DC`

**驗證方式**：`npm run lint`（`tsc --noEmit`）通過；用 Playwright 起 dev server 實際截圖檢查建立活動流程的週末標色、必填提示、警示角標。**尚未 commit。**

---

## 子專案 B — 活動頁面互動重構 🟡 設計已完成，待寫實作計畫

**對應項目**：#6、#7、#10、#12

**設計文件／實作計畫**：[subproject-B-design.md](./subproject-B-design.md)、[subproject-B-plan.md](./subproject-B-plan.md)（透過 superpowers brainstorming → writing-plans 流程產出，待執行）

**涉及檔案**：
- `src/mobile/EventScreen.tsx`（行動版容器，含 vote/heatmap/host 三個 Tab 的切換邏輯）
- `src/components/EventView.tsx`（桌面版容器，直接重用下面幾個 mobile 元件，Tab 邏輯目前是各自獨立實作）
- `src/mobile/VoteTab.tsx`（勾選時間畫面）
- `src/mobile/HeatmapTab.tsx`（熱點圖畫面，#12 的視覺重心調整也在這裡）
- `src/mobile/HostTab.tsx`（主辦人定案畫面，需要整合進 HeatmapTab 或改成熱點圖旁的區塊；#10 的「編輯活動資訊」入口也建議放這裡，跟 finalize/reopen/cancel 等既有主辦人操作放一起）
- `src/lib/api.ts`（`getUserNickname` / `getUserEmail`，識別資料已有基礎函式）
- `src/types.ts`（`ParticipantResponse` 已有 `nickname` / `email` 欄位，資料模型應該不用大改）
- `src/lib/localEventStore.ts`（**#10 需要新增一個 `updateEvent` 類的函式** — 目前只有 `createEvent`/`finalizeEvent`/`reopenEvent`/`cancelEvent`/`submitComment`/`submitResponse`，完全沒有「編輯已建立活動資訊」的寫入路徑，需要新增並比照其他主辦人操作用 `hostToken` 驗證權限）

**要處理的內容**：
- [ ] 無登入識別：比照 When2meet，在使用者勾選時間前強制先填「暱稱 + Email」
- [ ] 勾選完成後直接導向熱點圖頁面（不用手動切 Tab）
- [ ] 要修改回答時，用一個明確的按鈕/動作跳回勾選畫面，而不是常駐 Tab
- [ ] 取消 vote/heatmap（/host）之間的 Tab 切換設計
- [ ] 主辦人的「定案」按鈕整合到熱點圖畫面旁邊，不用獨立的 Tab/頁面
- [ ] **（#10）主辦人可編輯已建立的活動資訊**：新增 `localEventStore.ts` 的更新函式（`hostToken` 驗證）、主辦人視角提供「編輯活動」入口，表單建議直接重用子專案 D 做的 `CreateWizard.tsx`/`CreateEvent.tsx` 欄位（標題、截止時間、地點等），帶入既有資料做編輯而非重新建立
- [ ] **（#12）修正熱點圖頁面視覺重心**：使用者尚未勾選時間時，畫面不該讓「快去勾選時間」的 CTA 蓋過「熱點圖」這個頁面主題本身；跟上面的識別流程改動一起處理（強制先識別 → 勾選 → 才進熱點圖，這條路徑理順後，「尚未勾選」這個中介狀態的呈現方式要重新設計，不要讓它看起來像頁面的主要內容）

**備註**：這是影響範圍最大的一塊，會改變核心互動流程。因為桌面版的 `EventView.tsx` 是直接重用 mobile 元件，這裡的改動大部分只需要做一次；但兩邊 Tab 容器（`EventScreen.tsx` / `EventView.tsx`）本身的外層排版邏輯要分開處理。#10 的編輯表單如果重用子專案 D 的元件，實作順序上可能需要 D 的表單先完成／可重用。

---

## 子專案 C — 版面密度與留言板調整 ⬜ 未開始（依賴 B）

**對應項目**：#3、#4、#5、#11

**涉及檔案**：
- 同子專案 B 的檔案（`EventScreen.tsx` / `EventView.tsx` / `HeatmapTab.tsx` / `VoteTab.tsx` / `HostTab.tsx`）
- `src/mobile/CommentBoard.tsx`
- `src/mobile/VoteTab.tsx`（#11「批次快速勾選」標籤/功能所在位置，需要加上引導說明）

**要處理的內容**：
- [ ] 降低單一頁面資訊密度：調整元件尺寸（size）、版面排版、顏色配置，減少緊密壓迫感（不一定要拆頁面）
- [ ] Review 並移除不必要的指引小字（類似 AI 自動生成的說明文字）
- [ ] 留言板加上提示：「在這邊留言會被寄信喔」
- [ ] 留言板呈現位置調整到熱力圖（HeatmapTab）下方
- [ ] （簡報/Demo 面向，非程式改動）Demo 展示時挑選呼應四大核心特點（跨平台免登入、智慧橋時間、資訊同步、自動收尾）的一兩張重點畫面即可，不用展示完整 RWD 流程
- [ ] **（#11）「批次快速勾選」加上 onboarding／輔助說明**：標籤本身不好懂（使用者會困惑系統要怎麼幫忙勾選），點開後才發現好理解；不是 MVP 必要項目，但建議加簡短引導文字或首次使用提示，降低初次接觸的困惑感

**備註**：建議等子專案 B 的新版面（拿掉 Tab 切換）定案後再做，因為留言板位置、資訊密度調整都是基於 B 之後的新版面配置。

---

## 子專案 D — 建立活動流程優化 ✅ 已完成（#2 待使用者手動確認）

**對應項目**：#1、#2、#8

**設計文件／實作計畫**：[subproject-D-design.md](./subproject-D-design.md)、[subproject-D-plan.md](./subproject-D-plan.md)（透過 superpowers brainstorming → writing-plans → subagent-driven-development 流程產出並執行）

**涉及檔案**：
- `src/mobile/CreateWizard.tsx`（行動版建立活動精靈）
- `src/components/CreateEvent.tsx`（桌面版建立活動表單）
- `src/types.ts`（新增 `EventLocation` 型別、`EventData.location`、`CreateEventInput.location`）
- `src/lib/location.ts`（新檔，Google Maps 連結偵測與模擬解析邏輯）
- `src/lib/localEventStore.ts`（`createEvent()` 讀寫 `location` 欄位）
- `src/mobile/HeatmapTab.tsx`／`src/mobile/HostTab.tsx`／`src/mobile/FinalizedView.tsx`（桌面/行動共用的顯示端，各改一次）
- `src/components/ShareModal.tsx`／`src/mobile/ShareModal.tsx`（分享文字顯示端，各自獨立檔案，各改一次）

**處理內容 / 執行紀錄**：
- [x] **（#1）** 移除建立活動流程中「只選日期」/「需要選時段」的模式 Tab；改成每個已選日期旁的「+ 新增時段」按鈕，點擊後把整個表單升級為需選時段模式（影響全部已選日期），並提供「◀ 改回只選日期」的降級連結
- [ ] **（#2）** 調查後發現現有程式碼（新增時段表單＋已新增時段清單合併呈現、新增動作不覆蓋既有時段）看起來已經解決原始回饋描述的問題，可能是先前 commit（`83aaa4d`）已處理。**本次未改動**，待使用者實際操作 dev server 驗證後回報是否仍需微調
- [x] **（#8）** 新增獨立的「地點」欄位（`EventLocation { text, url? }`），支援輸入純文字或偵測貼上的 Google Maps 連結：完整連結同步解出地點名稱＋超連結；短連結（`maps.app.goo.gl` 等）因為專案純 client-side、無後端（見 CLAUDE.md），暫時用固定假地點名稱模擬解析過程（架構上已預留 `mockResolveShortLink()` 單一函式，未來要接真的後端/API 只需替換這個函式）。地點會出現在活動頁面的熱點圖資訊卡、主辦人定案備註預設值、行事曆匯出、LINE 分享文字等所有原本顯示 `description` 的地方；原有「地點或說明」欄位改標為「活動說明（選填）」的純自由文字欄位，跟地點分開

**驗證方式**：每個任務都通過 `npm run lint`（`tsc --noEmit`）；因為沒有瀏覽器工具可用，程式邏輯（模式升降級、Maps 連結偵測解析、樂觀更新）都是人工逐行追蹤驗證，**尚未經過真人瀏覽器手動走一遍**。最終全分支 review 抓到兩個 plan 本身的 bug 並已修正：①短連結解析中如果提早送出表單會靜默遺失地點資料；②部分常見 Google Maps 網址格式（如 `/maps/search/`、`.com.tw` 網域）解析不到地點名稱時，會把整串網址存成顯示文字，導致分享文字重複出現網址、UI 也可能爆版。兩者都已修正並經過 scoped re-review 確認。

**已知擱置事項**（低風險，可日後再處理）：
- 表單 unmount 時未取消進行中的短連結模擬解析計時器（React 19 下無害，只有接上真後端、解析變慢時才會比較有感）
- 定案（finalized）後的活動頁面目前不會顯示地點連結（`FinalizedView` 沒有顯示地點的區塊）——是否要在定案後也顯示地點是產品決策，留待之後評估
- Demo/種子活動資料目前都沒有帶 `location` 範例，新的地點 UI 沒有 demo 涵蓋

**備註**：跟子專案 B/C 是完全不同的流程（建立 vs. 回覆事件），彼此沒有依賴，可以隨時插隊執行，這次也確實是插隊先做的。子專案 B 的「#10 主辦人編輯活動資訊」功能可以重用這裡新做的表單元件（含地點欄位）。

---

## 建議執行順序

B → C → D，或 D 隨時插隊（跟 B/C 無依賴）。C 一定要排在 B 之後。

D 已完成插隊執行（2026-08-24）。接下來建議回到 B → C 的順序繼續。
